<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.demouno">

    <!--Recordemos que aquí tienen que aparecer todas las actividades que se esten trabajando!-->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DemoUno">
        <!--Aqui existia un warning y era debido a que de manera automatica se escribe en html y por consecuencia se consideraba
        vacial la llave con indicar al final el "/" es suficiente para eliminar el warning!-->
        <activity android:name=".activity_editor_de_correo" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>